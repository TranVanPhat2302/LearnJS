<!DOCTYPE html>
<html lang="en" dir="ltr">

<head>
  <meta charset="utf-8">
  <header><%= title %></header>
  <link rel="stylesheet" type="text/css"  href="../stylesheets/style.css" >
</head>

<body>
  <div id="container" class="header">
    <form action="/todolist" method="POST">
      <label for="tdate">Date</label><br><br>
      <input id="fdate" type="date" name="fdate"><br><br><br><br>
      <label for="tname">What you do</label><br><br>
      <input type="text" id="fname" name="fname" placeholder="Add task"><br><br>
      <input type="submit" value="Add" name="Addbtn"><br><br>

      <% if (success === true) { %>
      <script>
        alert("Added Success")
      </script>
      <%} %>
      
    
    </form>


    <div>
      <% if(items && items.length > 0 ) { %>
      <!-- <% console.log(items.length) %> -->
      <% var date = new Date() %>
      <% console.log(typeof(date)) %>
      <ul>
        <% for(var i = 1; i <= items.length; i ++) { %>
        <li> <span> <i class="fa fa-trash"></i> </span><%= items[i-1].tddate.getDate() %>-<%= items[i-1].tddate.getMonth() %>-<%= items[i-1].tddate.getFullYear() %>  :    <%= items[i-1]['tdname'] %> </li>
        <% } %>
      </ul>
      <% } %>
    </div>
  </div>

  <script type="text/javascript" src="../javascripts/script.js"></script>
</body>

</html>